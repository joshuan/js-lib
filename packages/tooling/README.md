# @joshuan/tooling

Strict presets for the common Next/Nest/Clean Architecture repository layout. The ESLint and
Vitest exports are factories because application paths, test setup, FSD layers, and coverage floors
are product choices.
